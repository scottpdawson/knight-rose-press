<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">
	{% include "components/head.njk" %}
	<body>
		<header>
			<img src="/static/img/krp_thumb.jpg" alt="Knight Rose Press" />
			<a href="{{ '/' | url }}">{{ metadata.title }}</a>
		</header>
		<main>
			{{ layoutContent | safe }}
		</main>
	</body>
</html>